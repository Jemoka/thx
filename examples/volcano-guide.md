# Volcano dispatch

The maintained guide is [Volcano dispatch](../docs/How-to/volcano.md).
See [dispatch.yaml](dispatch.yaml) for an inventory with SSH, SLURM, and
Volcano hosts. All providers consume the same execution YAML.
